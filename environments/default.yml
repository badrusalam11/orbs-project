# Default Environment Configuration
# This is the default configuration loaded when no specific environment is set

# Application URLs
cura_url: "https://katalon-demo-cura.herokuapp.com/"
api_url: "https://api.example.com"

# Credentials (use environment variables for sensitive data in production)
username: "test_user"
password: "test_password"

# API Configuration
api_timeout: 30
api_retry: 3

# Database Configuration
db_host: "localhost"
db_port: 5432
db_name: "test_db"
